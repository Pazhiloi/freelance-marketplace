import React, { FC } from 'react';
import Home from '../app/components/screens/home/Home';

const HomePage: FC = () => {
  return (
    <div>
      <Home/>
    </div>
  );
};

export default HomePage;