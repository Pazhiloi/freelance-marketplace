import React, { FC } from 'react';
import ChatScreen from '../app/components/screens/chat/ChatScreen';

const ChatPage: FC = () => {
  return (
    <div>
      <ChatScreen/>
    </div>
  );
};

export default ChatPage;