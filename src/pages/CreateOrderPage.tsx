import React, { FC } from 'react';
import CreateOrderScreen from '../app/components/screens/create-order/CreateOrderScreen';

const CreateOrderPage: FC = () => {
  return (
    <div>
      <CreateOrderScreen/>
    </div>
  );
};

export default CreateOrderPage;