import React, { FC } from 'react';
import Create from '../../sections/CreateWork/Create';

const CreateWork: FC = () => {
  return (
    <div className='container'>
      <Create/>
    </div>
  );
};

export default CreateWork;